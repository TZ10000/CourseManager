                // for (auto y : x.second->preReqNum)  // push its preReq to the deletelist
                // {
                //     if (y.at(0) != "None")  // if the preReq doesn't == None
                //         coursesDelete.push_back(y);
                // }